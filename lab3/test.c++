#include <iostream>
#include <ncurses.h>
using namespace std;




int main() {
//    initscr();              // Start ncurses mode

    // Move and print multiple messages
    // move(4, 4);
    // printw("5");

    // move(5, 5);
    // printw("6");

    // move(6, 6);
    // printw("7");

    // move(7, 7);
    // printw("8");

    // refresh();              // Draw all messages at once

    // getch();                // Wait for user input
    // endwin();               // End ncurses mode
    if(1%5!=0)
        {
            cout<<"success";
        }
    return 0;
}
